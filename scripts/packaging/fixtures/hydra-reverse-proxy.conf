# Hydra Reverse Proxy configuration file example

app_id = "hydra"
hydra_servers = ["http://localhost:7772"]
proxy_addr = ":3000"
[HydraClient]
apps_cache_duration = 20000
duration_between_all_servers_retry = 0
hydra_servers_cache_duration = 60000
max_number_of_retries = 10